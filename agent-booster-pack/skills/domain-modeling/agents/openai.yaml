interface:
  display_name: "ABP: Domain Modeling"
  short_description: "Shape data and effects first"
  default_prompt: "Use $domain-modeling to model data shapes, invariants, and boundaries before writing transformations."
