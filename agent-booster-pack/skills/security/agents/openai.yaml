interface:
  display_name: "ABP: Security"
  short_description: "Review trust boundaries and risk"
  default_prompt: "Use $security to review this change for auth, input, secrets, and dependency risk."
