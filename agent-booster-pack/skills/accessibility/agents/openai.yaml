interface:
  display_name: "ABP: Accessibility"
  short_description: "Verify inclusive, accessible UI"
  default_prompt: "Use $accessibility to review this interface for WCAG, keyboard, focus, and screen-reader issues."
