interface:
  display_name: "ABP: Deployment"
  short_description: "Reduce deployment toil safely"
  default_prompt: "Use $deployment to prepare release checks, rollout notes, gates, and rollback steps without mutating shared environments."
