# ABP Workflow

Use the bundled ABP `workflow` skill for this request.

Treat any text after the command as task context. Route the work, name the risk
profile, and select only the skills needed for a correct, idiomatic result.
