# Cart Total Bug

Fix `totalCents` in `src/cart.js`.

Requirements:

- Return the final total in cents after applying `SAVE10` as a 10 percent discount.
- Accept only arrays of items with non-negative integer `priceCents` and non-negative integer `quantity`.
- Throw a useful error for invalid item values.
- Keep the implementation small and focused.

Do not add external dependencies.
