# Cart Total Bug

Fix `totalCents` in `src/cart.js`.

Requirements:

- Return the final total in cents after applying the supported discount code.
- Reject malformed carts and invalid item values with useful errors.
- Keep the implementation small and focused.

Do not add external dependencies.
