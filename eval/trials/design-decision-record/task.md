# Checkout Configuration Note

The team is split on where checkout validation should live.

Create `docs/checkout-validation.md` that records a concrete decision for the
next implementation pass.

The note should cover:

- the context and problem being solved;
- the chosen boundary for validation;
- at least two alternatives considered;
- consequences or follow-up work.

Do not add external dependencies.
